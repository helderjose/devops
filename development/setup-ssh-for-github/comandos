https://help.github.com/en/github/authenticating-to-github/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent


Esses comandoes estão no site

ssh-keygen -t rsa -b 4096 -C "helderjosemelo@gmail.com"
pressioner enter nas opções para aceitar o valor padrão

eval "$(ssh-agent -s)"

no site, clique no link "Add the SSH key to your GitHub account."
sudo dnf install xclip
obs: o xclip copia para o clipboard "é um ctrl + c por linha de comando".
xclip -sel clip < ~/.ssh/id_rsa.pub
Logue no github -> settings -> ssh and gpg keys -> new ssh key ->
preencha o "title"
cole a chave no campo key

